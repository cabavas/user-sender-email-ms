services:
  mysql:
    image: mysql:8.0
    container_name: ms-email-db
    environment:
      MYSQL_DATABASE: ms-email-db
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3308:3306"
    volumes:
      - db_data:/var/lib/mysql
    restart: unless-stopped

volumes:
  db_data: